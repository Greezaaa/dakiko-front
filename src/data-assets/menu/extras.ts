import { Category } from "@enums/category.enum";
import { Extra } from "@interfaces/menu.interface";

export const extras: Extra = {
  name: "Ingrediente extra",
  price: 2.0,
  category: Category.EXTRAS,
};
